const GoogleImageGenerator = require("./google-image");
const FalImageGenerator = require("./fal-image");
const KieImageGenerator = require("./kie-image");
const TextGenerator = require("./text-generator");

module.exports = {
  GoogleImageGenerator,
  FalImageGenerator,
  KieImageGenerator,
  TextGenerator,
};
